# Firebase config (contains credentials — never commit)
firebase-config.js

# Claude Code local settings
.claude/

# Google Drive config (contains Client ID — never commit)
drive-config.js

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log
npm-debug.log*

# Node (if using build tools)
node_modules/
package-lock.json

# Env
.env
.env.local
