# Firebase credentials — local dev only, contains teamPassword (never commit)
firebase-credentials.js

# Claude Code local settings
.claude/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log
npm-debug.log*

# Node (if using build tools)
node_modules/
package-lock.json

# Tauri build output — generated by scripts/tauri-copy-dist.js
dist/

# Env
.env
.env.local

# Tauri updater signing keys — NEVER commit (private key is a secret)
src-tauri/.updater-private.key
src-tauri/.updater-private.key.pub
src-tauri/target/
